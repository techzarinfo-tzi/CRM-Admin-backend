PORT=5001
MONGO_URI=mongodb://127.0.0.1:27017/crm-admin
JWT_SECRET=change_this_secret
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173,http://localhost:3000

# used only by `npm run seed:admin` to create/update the default admin user
ADMIN_EMAIL=admin@crm-admin.local
ADMIN_PASSWORD=change_this_password

# Lead notification email (sent on every Contact Us submission)
EMAIL_USER=
EMAIL_PASS=
LEAD_NOTIFY_EMAIL=
